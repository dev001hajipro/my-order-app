import React from "react";

/**
 * SignUp form. it regist user to firebase.
 * 
 * @see https://firebase.google.com/docs/auth/web/start
 * @returns 
 */
export const SignUp: React.FC = () => {

    return (
        <div>
            <h1>ユーザー登録</h1>
        </div>
    )
}